apps:
  - name: http-ota-software-update
    env:
      global:
        executor_stacksize_required:
          - "200000"
        CARGO_ENV:
          - CONFIG_WIFI_PASSWORD='u2isdxm2'
          - CONFIG_WIFI_NETWORK='realme GT Neo2 5G'
          - CONFIG_NETWORK_MAX_CONCURRENT_SOCKETS=5
    selects:
      - network
      - random
      - hwrng
      - alloc

    conflicts:
      - ram-small
      # needs `feature(impl_trait_in_assoc_type)`
      - stable
