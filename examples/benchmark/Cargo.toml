[package]
name = "benchmark"
license.workspace = true
edition.workspace = true

publish = false

[lints]
workspace = true

[dependencies]
ariel-os = { path = "../../src/ariel-os", features = ["bench"] }
