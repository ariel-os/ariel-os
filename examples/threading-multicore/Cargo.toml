[package]
name = "threading-multicore"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
ariel-os = { path = "../../src/ariel-os", features = ["core-affinity"] }
ariel-os-chips = { path = "../../src/ariel-os-chips" }
