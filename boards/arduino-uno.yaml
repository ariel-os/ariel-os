version: 0.4.0
targets:
  arduino-uno:
    chip: atmega328p
    leds:
      # built-in LED on D13 (PB5)
      - color: orange
        pin: PB5
    buttons:
      # built-in button on D2 (PD2 / INT0)
      - pin: PD2
